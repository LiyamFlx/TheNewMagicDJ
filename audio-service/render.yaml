services:
  - type: web
    name: magic-dj-audio-service
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 2 --timeout 60 main:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.16